version: 2.1

workflows:
  test:
    jobs:
      - test

jobs:
  test:
    docker:
      - image: ryym/vim-ci-themis:v8.1.2000
    steps:
      - checkout
      - run: themis --version
      - run: themis test
